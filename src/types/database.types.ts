/**
 * Placeholder for Supabase-generated database types.
 * The Supabase client is used untyped (see src/lib/supabase.ts) because
 * generated types are not committed to the repo. Domain types live in
 * src/types/index.ts.
 */
export type Database = Record<string, never>;
